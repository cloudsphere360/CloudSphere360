pipeline {
    agent {
        label 'Jenkins Server Agent-1'
    }

    stages {

        stage('Checkout CloudSphere360 Source Code') {
            steps {
                checkout scm
            }
        }

    }
}